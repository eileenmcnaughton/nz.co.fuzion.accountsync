<?php

class CRM_Accountsync_Error {

  /**
   * Handle errors - this is mostly a placeholder for something clever that will potentially happen later
   * @param string $message
   * @param array $data
   * @param string $context
   */
  static function handleError($message, $data, $context) {
    //do whatever we are going to do with our errors :-)
  }
}